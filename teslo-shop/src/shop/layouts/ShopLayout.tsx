import { Outlet } from "react-router"

export const ShopLayout = () => {
    return (
        <>
            <main>
                <div>ShopLayout</div>
                <Outlet />
            </main>
        </>
    )
}
